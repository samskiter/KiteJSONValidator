language: objective-c
before_script:
    - export LANG=en_US.UTF-8
before_install:
    - brew update
    - brew uninstall xctool
    - brew install xctool
script:
    - xctool -workspace KiteJSONValidator.xcworkspace -scheme Tests -sdk iphonesimulator test ONLY_ACTIVE_ARCH=NO
